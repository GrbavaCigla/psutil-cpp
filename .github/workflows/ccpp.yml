name: C/C++ CI

on: [push]

jobs:
  build:

    runs-on: [ubuntu-latest, macos-latest, windows-latest]
    
    steps:
    - uses: actions/checkout@v1
    - name: configure
      run: cmake .
    - name: make
      run: make
